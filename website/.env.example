# DataMgmt Node Website Environment Variables
# These are only used during build time for Astro

# Site URL (used for canonical URLs and sitemap)
PUBLIC_SITE_URL=https://datamgmtnode.cryptuon.com

# API URL (relative path works for unified deployment)
# Only change if running website separately from node
PUBLIC_API_URL=/api
